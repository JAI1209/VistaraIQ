# VistaraIQ Auth Suite

## Backend

```bash
cd auth-suite/backend
cp .env.example .env
npm install
npm run dev
```

Runs on `http://localhost:5000` by default.

## Frontend

```bash
cd auth-suite/frontend
cp .env.example .env
npm install
npm run dev
```

Runs on `http://localhost:5173` by default.
